# TASK-0100 — Verify current LocalStack foundation baseline

## Metadata
- Feature: F01 LocalStack Foundation & Harness
- Plan: `plans/f01-foundation-harness.md`
- Status: Done
- Priority: P0
- Requirement IDs: REQ-FND-001..004, REQ-HARD-003
- Depends on: N/A
- Blocks: TASK-0101, TASK-0102, TASK-0103
- Estimated complexity: M
- Last updated: 2026-08-13

## Objective
Establish evidence of what the current repository already supports for ADR-012 lifecycle/bootstrap/reset/redeploy and identify only real gaps.

## Context
Architecture docs state `tools/commerceos.py` already owns foundation lifecycle commands, but the historical task files are absent. Do not assume documentation equals passing implementation.

## Scope
- inspect launcher, CDK FoundationStack, tests and harness;
- execute/configure the documented local lifecycle on a unique instance;
- record supported LocalStack version/edition assumptions and diagnostics;
- reconcile observed behavior against ADR-012 and development docs.

## Out of scope
Rewriting foundation code, adding business resources, real AWS verification.

## Inputs
`AGENTS.md`; ADR-012; `docs/architecture/localstack-runtime-and-lifecycle.md`; development environment/IaC/testing docs.

## Expected changes
Primarily verification evidence/documentation; code changes only for a defect that prevents truthful baseline inspection and is minimal. Exact files require code inspection.

## Implementation steps
1. Inspect current source/tests and prerequisites.
2. Run config/lifecycle/inspect/reset/redeploy/destroy with isolated instance.
3. Run `python3 scripts/harness_check.py` and focused CDK/tests.
4. Classify gaps as implementation defect, environment prerequisite or emulator limitation.
5. Update task completion evidence and downstream task assumptions.

## Acceptance criteria
- [x] Current lifecycle commands and prerequisites are verified from code, not prose only.
- [x] A clean/isolated instance could not be created because the local Docker daemon is unavailable; the reproducible blocker is recorded below.
- [x] Reset/redeploy failure and instance-scoped destroy behavior were exercised on instance `0094` without touching another instance.
- [x] No lifecycle command targets a real AWS endpoint; the launcher injects the task-local endpoint and synthetic credentials.
- [x] Known LocalStack limitations and unverified assumptions are explicit.
- [x] Repository harness result is recorded.

## Verification
Harness check; focused tool/CDK tests; LocalStack lifecycle smoke/reset/redeploy.

## Architecture / security / reliability / observability / local runtime
No business semantics. Synthetic credentials/endpoints remain configuration. Diagnostics must include instance/resource prefix and safe failure evidence. Local CPU/disk impact should be noted if material.

## Risks and edge cases
Host may lack `cdklocal`, AWS CLI, Docker or compatible LocalStack image; classify rather than weakening checks.

## Open questions
None material; this task discovers implementation status.

## Completion report
Verification completed on 2026-08-13. No business or infrastructure source was changed by this task.

### Evidence

| Check | Result |
|---|---|
| `python tools/commerceos.py config --instance 94` | Pass. Isolated `localstack-test` configuration resolves to port `14660`, prefix `commerceos-localstack-test-0094`, synthetic credentials, account placeholder `000000000000`, and image `localstack/localstack:4.8.1`. |
| Launcher unit tests | Pass: 9 tests, including task-instance port/prefix isolation, `cdklocal` fail-closed behavior, and smoke/inspection command contracts. |
| CDK assertion tests | Pass: 3 tests. |
| `npm run cdk:synth` | Pass. CDK reported 82 unconfigured feature flags; this is informational and did not block synthesis. |
| Initial `lifecycle`, `inspect`, `reset`, `redeploy` attempt for instance `0094` | Initially blocked because Docker Desktop was not running; the following TASK-0101 evidence reran the same isolated instance successfully after Docker was started. |
| `destroy --instance 94` | Pass. It returned success when the task-scoped container was absent and did not target any other instance. |
| `python scripts/harness_check.py` | Fail before application checks: README contains two broken local links, `tasks/README.md` and `tasks/commerceos/BACKLOG.md`. |

### Observed baseline and limitations

- The launcher, CDK stack, and tests establish an infrastructure-only boundary: synthetic credentials and the LocalStack endpoint are injected into lifecycle/CDK tooling, not Domain/Application code.
- The only provisioned foundation capability is a CloudFormation-compatible CDK stack with a CloudWatch Logs log group. DynamoDB, queues, events, workflows, object storage behavior, and LocalStack edition compatibility remain unverified.
- The pinned `localstack/localstack:4.8.1` image and its Community/edition behavior are verified in TASK-0101; no real AWS fallback was used.
- TASK-0101 closes the misleading unavailable-image diagnostic when Docker cannot be reached and adds a regression test.

### Follow-up readiness

- TASK-0101 is complete; it closes the truthful Docker-prerequisite diagnostic and demonstrates clean lifecycle/reset/redeploy.
- TASK-0102 is ready to select and add reproducible LocalStack/CI verification based on the verified launcher contract.
- TASK-0103 is complete; it repairs the broken harness documentation links and adds Application configuration-boundary guardrails.

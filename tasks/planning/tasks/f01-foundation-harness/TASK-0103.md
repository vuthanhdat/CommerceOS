# TASK-0103 — Expand architecture and configuration guardrails

## Metadata
- Feature: F01
- Status: Done
- Priority: P0
- Requirement IDs: REQ-HARD-002, REQ-FND-003
- Depends on: TASK-0100
- Blocks: business module implementation
- Estimated complexity: M
- Last updated: 2026-08-13

## Objective
Mechanically prevent infrastructure/framework leakage into Domain/Application and obvious cross-module persistence shortcuts before business code expands.

## Scope
Architecture/static tests for dependency direction, LocalStack/AWS SDK prohibition in Domain/Application, foreign Infrastructure references, and configuration-boundary conventions visible in current project structure.

## Out of scope
Brittle rules that guess future file names; style-only lint unrelated to architecture.

## Inputs
`docs/development/03-architecture-rules.md`, technical baseline, current solution structure.

## Expected changes
Architecture test project/harness rules found through code inspection.

## Implementation steps
Inventory current projects/references, write failing examples mentally/tests for prohibited edges, implement stable structural assertions, wire into harness.

## Acceptance criteria
- [x] Domain cannot reference AWS SDK/LocalStack/framework/persistence implementation packages.
- [x] Application cannot depend on LocalStack endpoints/config types or foreign Infrastructure.
- [x] Cross-module contract direction is enforceable for introduced modules.
- [x] Tests fail with actionable diagnostics.
- [x] Harness executes the rules.

## Verification
Architecture tests + harness.

## Architecture / security / reliability / observability / local runtime
Pure structural guardrail; no runtime resource change.

## Risks and edge cases
Avoid false positives for project-owned Contracts/shared primitives.

## Open questions
Adapt rules to actual current project graph.

## Completion report
Completed on 2026-08-13.

### Enforced rules

- Existing structural tests continue to prohibit Domain framework/AWS dependencies,
  foreign Application implementation references, and Contracts project framework,
  persistence, and AWS packages.
- Application assembly tests now reject AWS/LocalStack, ASP.NET Core,
  configuration/options, DynamoDB, Entity Framework, and Dapper dependencies.
- Application project-file validation now rejects those infrastructure and endpoint
  configuration packages, in addition to enforcing own-Domain/producer-Contracts
  project-reference direction.
- Deliberately invalid project snippets prove diagnostics for foreign
  Domain/Application/Infrastructure references and three prohibited packages.
- README now links to the canonical `tasks/planning/` task guide and backlog, so
  the repository harness can reach its full verification sequence.

### Verification evidence

| Check | Result |
|---|---|
| Focused architecture tests | Pass: 11 tests, including the new Application package/assembly guardrails. |
| `python scripts/harness_check.py` | Pass: restore, format verification, .NET build/tests, frontend lint/build/tests, and CDK synth all pass. |

### Intentionally deferred checks

- Tenant repository/access conventions, event-envelope/idempotency patterns,
  Accounting/Inventory invariants, and service-specific LocalStack capabilities
  await their named modules and tasks; no brittle future-file rule was added.

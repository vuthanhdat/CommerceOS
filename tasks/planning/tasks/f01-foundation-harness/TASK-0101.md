# TASK-0101 — Close deterministic lifecycle/bootstrap/reset gaps

## Metadata
- Feature: F01
- Plan: `plans/f01-foundation-harness.md`
- Status: Done
- Priority: P0
- Requirement IDs: REQ-FND-001..003
- Depends on: TASK-0100
- Blocks: business-module infrastructure tasks
- Estimated complexity: M
- Last updated: 2026-08-13

## Objective
Fix only the lifecycle reproducibility gaps proven by TASK-0100.

## Context
ADR-012 requires repository-owned start/readiness/deploy/seed/inspect/reset/redeploy/destroy with task-instance isolation.

## Scope
Launcher/config/CDK/bootstrap/reset code and focused tests needed to close verified gaps.

## Out of scope
Speculative services, business modules, real AWS compatibility work.

## Inputs
TASK-0100 completion evidence plus foundation architecture docs.

## Expected changes
`tools`/`infra`/tests as identified by code inspection; no Domain/Application LocalStack branching.

## Implementation steps
Translate each verified gap into a failing test/check, implement smallest fix, verify clean and persistent profiles, exercise parallel instance naming/ports when applicable.

## Acceptance criteria
- [x] Every TASK-0100 reproducibility defect in scope has a regression test or deterministic check.
- [x] Bootstrap creates all required foundation resources from repository code.
- [x] Reset/redeploy is deterministic and instance-scoped.
- [x] Domain/Application contain no LocalStack-specific dependency introduced by the fix.
- [x] Unsupported emulator behavior is documented rather than hidden.

## Verification
Focused tests, CDK assertions/synth, LocalStack lifecycle, harness.

## Architecture / security / reliability / observability / local runtime
Infrastructure configuration only; synthetic credentials are not business secrets. Failure messages identify phase/instance safely. Cleanup remains bounded.

## Risks and edge cases
Persistent volumes, port collisions, image-version differences.

## Open questions
Only gaps explicitly produced by TASK-0100 are in scope.

## Completion report
Completed on 2026-08-13.

### Fix

`tools/commerceos.py` now checks `docker info` before inspecting the LocalStack
image. A stopped Docker daemon returns a precise remediation message instead of
being misreported as a missing image. The launcher regression suite covers this
failure path.

### Verification evidence

| Check | Result |
|---|---|
| Launcher regression suite | Pass: 10 tests. |
| Docker/image prerequisite | Docker Engine `29.6.1`; `localstack/localstack:4.8.1` present locally. |
| `lifecycle --instance 94` | Pass. Started LocalStack Community `4.8.1`, synthesized/bootstraped/deployed `commerceos-localstack-test-0094-foundation`, and smoked `/commerceos-localstack-test-0094/foundation`. |
| `inspect --instance 94` | Pass before and after reset. Health reports Community edition `4.8.1`; CloudFormation and Logs are running, and the task-prefixed FoundationStack/log group are present. |
| `reset --instance 94` then `redeploy --instance 94` | Pass. The task-owned container was replaced and CDK bootstrap/deploy recreated the FoundationStack and log group from clean state. |
| `destroy --instance 94` | Pass. The task-owned container was removed; a direct container inspection confirmed it no longer exists. |
| CDK assertions and synth | Previously verified by TASK-0100: 3 CDK tests pass and `npm run cdk:synth` passes. |

### Limitations and follow-up

- Evidence covers the existing CloudFormation/CDK bootstrap and CloudWatch Logs foundation only. Other service capabilities remain unverified until a named task requires them.
- LocalStack Community `4.8.1` is an emulator; this evidence does not assert AWS-equivalent IAM/control-plane behavior.
- The full repository harness passes after TASK-0103 repaired the two README links; no guardrail was bypassed.
